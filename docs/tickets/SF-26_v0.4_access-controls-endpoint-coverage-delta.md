# SF-26: AccessControls Endpoint Coverage Delta

**Module:** `sharefile-sdk-client`
**Depends on:** SF-10
**Milestone:** v0.4 Endpoint Coverage
**Spec Reference:** [API_REFERENCE.md](../API_REFERENCE.md) &sect;8, [API_ENDPOINT_COVERAGE.md](../API_ENDPOINT_COVERAGE.md) AccessControls

## Goal

Add the remaining AccessControls endpoints missing after SF-10.

## Requirements

1. Add exact connectors:
   - `POST /sf/v3/AccessControls/BulkDeleteForPrincipal?principalId={principalId}`
   - `POST /sf/v3/Items({id})/AccessControls/NotifyUsersPreview`
2. Keep existing `bulkDelete(itemId, principalIds)` and `notifyUsers(itemId, ...)` behavior intact.
3. Return an appropriate typed response for notification preview if the API response is structured;
   otherwise document and test `Void` behavior.

## Acceptance Criteria

- Mock transport tests assert exact HTTP method, path, and query parameters for both endpoints.
- Existing SF-10 AccessControls tests continue to pass.
